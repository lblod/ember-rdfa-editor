{{#if @controller}}
  {{#if this.canToggle}}
    <Toolbar::Icon
      @active={{this.isActive}}
      @title={{t 'ember-rdfa-editor.unordered-list'}}
      @icon='unordered-list'
      {{on 'click' this.toggle}}
    />
  {{/if}}
{{/if}}