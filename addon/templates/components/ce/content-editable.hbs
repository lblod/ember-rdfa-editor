{{#if this.yieldHTML}}
  {{{yield}}}
{{else}}
  {{yield}}
{{/if}}
&nbsp;

<!-- components -->
{{#each this.components as |config|}}
  <!-- component {{config.id}} -->
  {{log config}}
  {{#ember-wormhole destinationElement=config.element}}
    {{ce/component-wrapper id=config.id name=config.name content=config.content removeComponent=(action 'removeComponent')}}
  {{/ember-wormhole}}
{{/each}}
