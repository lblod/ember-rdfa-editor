import htmlInputCleaner from 'dummy/utils/html-input-cleaner';
import { module, test } from 'qunit';

module('Unit | Utility | html-input-cleaner', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = htmlInputCleaner();
    assert.ok(result);
  });
});
