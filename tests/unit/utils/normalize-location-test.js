import normalizeLocation from 'dummy/utils/normalize-location';
import { module, test } from 'qunit';

module('Unit | Utility | normalizeLocation', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = normalizeLocation();
    assert.ok(result);
  });
});
