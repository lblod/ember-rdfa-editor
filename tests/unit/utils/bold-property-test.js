import boldProperty from 'dummy/utils/bold-property';
import { module, test } from 'qunit';

module('Unit | Utility | bold-property', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = boldProperty();
    assert.ok(result);
  });
});
