import rdfaBackspaceHandler from 'dummy/utils/rdfa-backspace-handler';
import { module, test } from 'qunit';

module('Unit | Utility | rdfa-backspace-handler', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = rdfaBackspaceHandler();
    assert.ok(result);
  });
});
