import makeNativeOrClassicInstance from 'dummy/utils/make-native-or-classic-instance';
import { module, test } from 'qunit';

module('Unit | Utility | make-native-or-classic-instance', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = makeNativeOrClassicInstance();
    assert.ok(result);
  });
});
