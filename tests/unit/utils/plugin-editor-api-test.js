import pluginEditorApi from 'dummy/utils/plugin-editor-api';
import { module, test } from 'qunit';

module('Unit | Utility | PluginEditorApi', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pluginEditorApi();
    assert.ok(result);
  });
});
