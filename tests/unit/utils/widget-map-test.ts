import widgetMap from 'dummy/utils/widget-map';
import { module, test } from 'qunit';

module('Unit | Utility | widget-map', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = widgetMap();
    assert.ok(result);
  });
});
