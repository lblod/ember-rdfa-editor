import documentInterface from 'dummy/utils/document-interface';
import { module, test } from 'qunit';

module('Unit | Utility | document-interface', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = documentInterface();
    assert.ok(result);
  });
});
