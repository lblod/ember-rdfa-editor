import underlineProperty from 'dummy/utils/underline-property';
import { module, test } from 'qunit';

module('Unit | Utility | underline-property', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = underlineProperty();
    assert.ok(result);
  });
});
