import italicProperty from 'dummy/utils/italic-property';
import { module, test } from 'qunit';

module('Unit | Utility | italic-property', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = italicProperty();
    assert.ok(result);
  });
});
