import pluginHintsRegistryApi from 'dummy/utils/plugin-hints-registry-api';
import { module, test } from 'qunit';

module('Unit | Utility | PluginHintsRegistryApi', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pluginHintsRegistryApi();
    assert.ok(result);
  });
});
