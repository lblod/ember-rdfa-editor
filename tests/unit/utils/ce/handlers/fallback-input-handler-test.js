import ceHandlersFallbackInputHandler from 'dummy/utils/ce/handlers/fallback-input-handler';
import { module, test } from 'qunit';

module('Unit | Utility | ce/handlers/fallback-input-handler', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = ceHandlersFallbackInputHandler();
    assert.ok(result);
  });
});
