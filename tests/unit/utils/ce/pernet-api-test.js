import pernetApi from 'dummy/utils/pernet-api';
import { module, test } from 'qunit';

module('Unit | Utility | pernet-api', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pernetApi();
    assert.ok(result);
  });
});
