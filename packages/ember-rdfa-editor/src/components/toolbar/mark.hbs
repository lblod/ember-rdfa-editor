{{! @glint-nocheck: not typesafe yet }}
{{#if @controller}}
  <Toolbar::Button
    @active={{this.isActive}}
    @title={{@title}}
    @icon={{@icon}}
    @disabled={{not this.canToggle}}
    {{on "click" this.toggle}}
  />
{{/if}}