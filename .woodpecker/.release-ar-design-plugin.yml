steps:
  install:
    image: node:24-slim
    commands:
      - npm config set ignore-scripts true
      - npm i -g corepack@0.35
      - pnpm i --frozen-lockfile
  release-next:
    image: node:24-slim
    commands:
      - npm config set ignore-scripts true
      - npm i -g corepack@0.35
      # pnpm doesn't handle this setting well, so we do it with npm
      - npm config set --location project '//registry.npmjs.org/:_authToken' $NPM_TOKEN
      - pnpm --filter @lblod/ember-rdfa-editor build
      - pnpm --filter @lblod/say-roadsign-regulation-plugin build
      - pnpm --filter @lblod/say-ar-design-plugin build
      - pnpm publish packages/say-ar-design-plugin --access public --tag next --no-git-checks
    environment:
      NPM_TOKEN:
        from_secret: npm_access_token
    when:
      ref:
        include: refs/tags/@lblod/say-ar-design-plugin@*-next.*
  release:
    image: node:24-slim
    commands:
      - npm config set ignore-scripts true
      - npm i -g corepack@0.35
      # pnpm doesn't handle this setting well, so we do it with npm
      - npm config set --location project '//registry.npmjs.org/:_authToken' $NPM_TOKEN
      - pnpm --filter @lblod/ember-rdfa-editor build
      - pnpm --filter @lblod/say-roadsign-regulation-plugin build
      - pnpm --filter @lblod/say-ar-design-plugin build
      - pnpm publish packages/say-ar-design-plugin --access public --tag latest --no-git-checks
    environment:
      NPM_TOKEN:
        from_secret: npm_access_token
    when:
      ref:
        exclude: refs/tags/@lblod/say-ar-design-plugin@*-*
when:
  event: tag
  ref:
    include: refs/tags/@lblod/say-ar-design-plugin@*
    exclude: refs/tags/@lblod/say-ar-design-plugin@*-dev.*
