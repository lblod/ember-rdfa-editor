steps:
  install:
    image: node:22-slim
    commands:
      - corepack enable
      - pnpm i --frozen-lockfile
  lint-js:
    image: node:22-slim
    group: lint
    commands:
      - corepack enable
      - pnpm lint:js
  lint-hbs:
    image: node:22-slim
    group: lint
    commands:
      - corepack enable
      - pnpm lint:hbs
  lint-prettier:
    image: node:22-slim
    group: lint
    commands:
      - corepack enable
      - pnpm lint:prettier
  test:
    image: danlynn/ember-cli:6.1.0
    commands:
      - corepack enable
      - pnpm test:ember
  prepack:
    image: node:22-slim
    commands:
      - corepack enable
      - pnpm prepack
when:
  event:
    - pull_request
