matrix:
  scenario:
    - ember-lts-3.28
    - embroider-safe
steps:
  - name: ${scenario}
    image: danlynn/ember-cli:4.8.0
    commands:
      - npm ci
      - npx ember try:one ${scenario}
when:
  event:
    - pull_request
